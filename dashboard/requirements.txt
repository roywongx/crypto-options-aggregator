# Crypto Options Aggregator - Python Dependencies
# 安装: pip install -r requirements.txt

# === Web Framework ===
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# === HTTP Client ===
httpx>=0.27.0,<1.0.0

# === WebSocket ===
websockets>=12.0

# === Data Validation ===
pydantic>=2.0.0

# === Scientific Computing (核心金融计算强依赖) ===
# quant_engine.py, grid_engine.py, scan_engine.py 等模块直接依赖 scipy
# 缺少 scipy 会导致 Greeks 计算、IV 插值、统计分位数等功能异常
scipy>=1.10.0

# === AI Integration (可选但推荐) ===
# 如需 AI 辩论分析和 Copilot 功能，请安装:
# litellm>=1.0.0
